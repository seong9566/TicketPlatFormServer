# 구매/판매 내역 기능 개발 계획서

## 📋 문서 정보
- **작성일**: 2026-02-09
- **프로젝트**: TicketPlatFormServer
- **기능**: 구매 내역 및 판매 내역 조회 API
- **상태**: ✅ 핵심 기능 구현 완료

---

## 1. 개요

티켓 거래 플랫폼에서 사용자가 자신의 구매 내역과 판매 내역을 조회할 수 있는 기능입니다. 커서 기반 페이지네이션과 다양한 필터링 옵션을 지원하여 사용자가 원하는 거래 정보를 효율적으로 확인할 수 있습니다.

### 1.1 주요 기능
- 구매 내역 조회 (사용자가 구매자인 거래)
- 판매 내역 조회 (사용자가 판매자인 거래)
- 상태별 필터링 (예약, 결제대기, 완료, 취소 등)
- 기간별 필터링 (1주일, 1개월, 3개월, 6개월, 전체)
- 정렬 기능 (최신순, 오래된순)
- 커서 기반 페이지네이션

---

## 2. 현재 구현 상태 (✅ 완료)

### 2.1 API 엔드포인트

**파일**: `TicketPlatFormServer/Controllers/TransactionController.cs`

| 메서드 | 엔드포인트 | 설명 | 인증 |
|--------|-----------|------|------|
| GET | `/api/transactions/purchases` | 구매 내역 조회 | Required |
| GET | `/api/transactions/sales` | 판매 내역 조회 | Required |

**쿼리 파라미터**:
```csharp
- status: string (쉼표로 구분, 예: "paid,confirmed")
- period: string (1w, 1m, 3m, 6m, all)
- sortBy: string (latest, oldest)
- cursor: string (Base64 인코딩된 페이지네이션 커서)
- limit: int (기본값: 20, 최대값: 50)
```

### 2.2 서비스 레이어

**인터페이스**: `Services/Transaction/ITransactionService.cs`
**구현체**: `Services/Transaction/TransactionService.cs`

```csharp
Task<TransactionHistoryRespDto> GetPurchaseHistoryAsync(
    long userId,
    string? status,
    string? period,
    string? sortBy,
    string? cursor,
    int? limit
);

Task<TransactionHistoryRespDto> GetSalesHistoryAsync(
    long userId,
    string? status,
    string? period,
    string? sortBy,
    string? cursor,
    int? limit
);
```

**주요 로직**:
- 기본 페이지 크기: 20개
- 최대 페이지 크기: 50개
- 커서 인코딩/디코딩 (Base64 JSON)
- 거래 상태 코드 및 기간값 검증
- 예외 처리 (AppException)

### 2.3 데이터베이스 모델

**핵심 엔티티**:

| 엔티티 | 파일 | 설명 |
|--------|------|------|
| `Transaction` | `DBModel/Transaction.cs` | 거래 메인 테이블 (구매자, 판매자, 금액, 상태) |
| `TransactionItem` | `DBModel/TransactionItem.cs` | 거래 항목 (티켓별 수량, 단가, 소계) |
| `TransactionHistory` | `DBModel/TransactionHistory.cs` | 거래 상태 변경 이력 |
| `TransactionStatus` | `DBModel/TransactionStatus.cs` | 거래 상태 코드 마스터 |
| `ChatRoom` | `DBModel/ChatRoom.cs` | 채팅방 (거래와 1:1 연결) |

**거래 상태 코드**:
```
- reserved: 예약됨
- pending_payment: 결제 대기
- paid: 결제 완료
- confirmed: 구매 확정
- completed: 거래 완료
- cancelled: 취소됨
- refunded: 환불됨
```

### 2.4 DTO (데이터 전송 객체)

**파일**: `DTO/Transaction/TransactionHistoryRespDto.cs`

```csharp
// 응답 컨테이너
public class TransactionHistoryRespDto
{
    public List<TransactionHistoryItemDto> Items { get; set; }
    public string? NextCursor { get; set; }
    public bool HasMore { get; set; }
    public int TotalCount { get; set; }
}

// 개별 거래 항목
public class TransactionHistoryItemDto
{
    // 거래 기본 정보
    public long TransactionId { get; set; }
    public int TicketId { get; set; }
    public string TicketTitle { get; set; }
    public string? TicketThumbnailUrl { get; set; }

    // 이벤트 정보
    public DateTime EventDateTime { get; set; }
    public string? VenueName { get; set; }
    public string? SeatInfo { get; set; }

    // 거래 상세
    public int Quantity { get; set; }
    public int UnitPrice { get; set; }
    public int TotalAmount { get; set; }

    // 상태 정보
    public string StatusCode { get; set; }
    public string StatusName { get; set; }

    // 거래 상대방 (조건부)
    public TransactionUserDto? Buyer { get; set; }   // 판매 내역에서만
    public TransactionUserDto? Seller { get; set; }  // 구매 내역에서만

    // 채팅방
    public long? RoomId { get; set; }

    // 타임스탬프
    public DateTime CreatedAt { get; set; }
    public DateTime? PaidAt { get; set; }
    public DateTime? ConfirmedAt { get; set; }
    public DateTime? CancelledAt { get; set; }
}

// 거래 상대방 정보
public class TransactionUserDto
{
    public long UserId { get; set; }
    public string Nickname { get; set; }
    public string? ProfileImageUrl { get; set; }
}
```

### 2.5 리포지토리 (데이터 접근 계층)

**인터페이스**: `Repository/Transaction/ITransactionHistoryRepository.cs`
**구현체**: `Repository/Transaction/TransactionHistoryRepository.cs`

**기술 스택**:
- ORM: **Dapper** (원시 SQL 쿼리)
- 데이터베이스: **MySQL**

**쿼리 특징**:
- 복잡한 LEFT JOIN (6개 테이블)
  - transactions
  - transaction_items
  - tickets
  - events
  - seats
  - users (buyer/seller)
  - chat_rooms
  - payment_details
- 필터링 조건:
  - 상태별: `t.status_code IN (@StatusCodes)`
  - 기간별: `t.created_at >= @StartDate`
  - 삭제된 항목 제외: `t.deleted_at IS NULL`
- 커서 기반 페이지네이션:
  ```sql
  WHERE (t.created_at < @CursorCreatedAt
         OR (t.created_at = @CursorCreatedAt AND t.id < @CursorId))
  ```
- 정렬: `ORDER BY t.created_at DESC, t.id DESC` (또는 ASC)

### 2.6 의존성 주입 (DI)

**파일**: `Program.cs` (라인 270-271)

```csharp
builder.Services.AddScoped<ITransactionHistoryRepository, TransactionHistoryRepository>();
builder.Services.AddScoped<TicketPlatFormServer.Services.Transaction.ITransactionService,
    TicketPlatFormServer.Services.Transaction.TransactionService>();
```

### 2.7 API 명세 문서

**파일**: `TicketPlatFormServer/api_spec/sales_purchases_api.md`

- 전체 API 요청/응답 예시
- 상태값 및 기간값 정의
- Flutter UI 사용 시나리오 (탭 구성)
- 에러 응답 정의

---

## 3. 아키텍처 다이어그램

```
[Client]
    |
    | HTTP GET /api/transactions/purchases
    | HTTP GET /api/transactions/sales
    v
[TransactionController]
    |
    | 인증 검증 ([Authorize])
    | 파라미터 추출
    v
[TransactionService]
    |
    | 파라미터 검증 및 변환
    | 커서 디코딩
    | 비즈니스 로직 처리
    v
[TransactionHistoryRepository]
    |
    | SQL 쿼리 생성
    | Dapper 실행
    | 결과 매핑
    v
[MySQL Database]
    |
    | transactions
    | transaction_items
    | tickets, events, seats
    | users
    | chat_rooms
    | payment_details
    |
    v
[TransactionHistoryRespDto]
    |
    | 페이지네이션 메타데이터
    | 거래 항목 리스트
    v
[Client]
```

---

## 4. 개선 및 보완 계획

### 4.1 에러 처리 강화 (우선순위: 높음)

**현재 문제점**:
- API 명세에는 정의되어 있으나 코드에서 유효성 검증이 미흡
- 예외 처리가 제한적 (try-catch 부족)

**개선 사항**:

#### 4.1.1 파라미터 유효성 검증
```csharp
// TransactionService.cs

// ✅ 추가 필요: 상태 코드 검증
private void ValidateStatusCodes(string? status)
{
    if (string.IsNullOrEmpty(status)) return;

    var validStatuses = new[] {
        "reserved", "pending_payment", "paid",
        "confirmed", "completed", "cancelled", "refunded"
    };

    var inputStatuses = status.Split(',', StringSplitOptions.RemoveEmptyEntries);
    var invalidStatuses = inputStatuses.Except(validStatuses);

    if (invalidStatuses.Any())
    {
        throw new AppException(
            ErrorCode.INVALID_PARAMETER,
            $"Invalid status codes: {string.Join(", ", invalidStatuses)}"
        );
    }
}

// ✅ 추가 필요: 기간 검증
private void ValidatePeriod(string? period)
{
    if (string.IsNullOrEmpty(period)) return;

    var validPeriods = new[] { "1w", "1m", "3m", "6m", "all" };

    if (!validPeriods.Contains(period))
    {
        throw new AppException(
            ErrorCode.INVALID_PARAMETER,
            $"Invalid period: {period}. Valid values: {string.Join(", ", validPeriods)}"
        );
    }
}

// ✅ 추가 필요: 정렬 검증
private void ValidateSortBy(string? sortBy)
{
    if (string.IsNullOrEmpty(sortBy)) return;

    var validSortBy = new[] { "latest", "oldest" };

    if (!validSortBy.Contains(sortBy))
    {
        throw new AppException(
            ErrorCode.INVALID_PARAMETER,
            $"Invalid sortBy: {sortBy}. Valid values: {string.Join(", ", validSortBy)}"
        );
    }
}
```

#### 4.1.2 예외 처리 추가
```csharp
// TransactionService.cs

public async Task<TransactionHistoryRespDto> GetPurchaseHistoryAsync(...)
{
    try
    {
        // ✅ 파라미터 검증 추가
        ValidateStatusCodes(status);
        ValidatePeriod(period);
        ValidateSortBy(sortBy);

        // 기존 로직...
    }
    catch (AppException)
    {
        throw; // AppException은 그대로 전파
    }
    catch (JsonException ex)
    {
        // 커서 파싱 에러
        throw new AppException(ErrorCode.INVALID_PARAMETER, "Invalid cursor format", ex);
    }
    catch (Exception ex)
    {
        // 예상치 못한 에러
        _logger.LogError(ex, "Failed to get purchase history for user {UserId}", userId);
        throw new AppException(ErrorCode.INTERNAL_ERROR, "Failed to retrieve purchase history", ex);
    }
}
```

#### 4.1.3 리포지토리 예외 처리
```csharp
// TransactionHistoryRepository.cs

public async Task<List<TransactionHistoryItemDto>> GetPurchaseHistoryAsync(...)
{
    try
    {
        using var connection = _dapperContext.CreateConnection();

        // 쿼리 실행...
        var result = await connection.QueryAsync<TransactionHistoryItemDto>(...);

        return result.AsList();
    }
    catch (MySqlException ex) when (ex.Number == 1205) // Lock timeout
    {
        _logger.LogWarning(ex, "Database lock timeout while fetching purchase history");
        throw new AppException(ErrorCode.DATABASE_TIMEOUT, "Database is busy. Please try again.", ex);
    }
    catch (MySqlException ex)
    {
        _logger.LogError(ex, "Database error while fetching purchase history");
        throw new AppException(ErrorCode.DATABASE_ERROR, "Failed to query purchase history", ex);
    }
}
```

### 4.2 추가 필드 지원 (우선순위: 중간)

**현재 누락된 필드**:

#### 4.2.1 환불 정보
- `RefundedAt` (환불 완료 시각)
- `RefundReason` (환불 사유)
- `RefundAmount` (환불 금액)

**구현 위치**:
- DTO에 필드 추가: `TransactionHistoryItemDto.cs`
- Repository SQL 쿼리 수정: LEFT JOIN `refund_details`

#### 4.2.2 자동 확정 예정 시각
- `AutoConfirmAt` (자동 구매 확정 예정 시각)

**구현 위치**:
- DTO에 필드 추가: `TransactionHistoryItemDto.cs`
- Repository SQL 쿼리에 `t.auto_confirm_at` 컬럼 포함

#### 4.2.3 전체 거래 금액
- `TransactionAmount` (전체 거래 금액, Transaction.amount)
- 현재는 TransactionItem의 TotalAmount만 포함

#### 4.2.4 결제 방법 상세
- `PaymentMethod` (결제 수단: 신용카드, 계좌이체 등)
- `PaymentProvider` (PG사: 토스페이, 카카오페이 등)

**구현 위치**:
- DTO에 필드 추가
- Repository SQL에서 `payment_details` 테이블 활용

### 4.3 성능 최적화 (우선순위: 중간)

#### 4.3.1 쿼리 성능 개선

**문제점**:
- 6개 테이블 LEFT JOIN으로 쿼리 복잡도 높음
- 인덱싱 전략 미검증

**개선 방안**:

**1) 인덱스 추가**
```sql
-- transactions 테이블
CREATE INDEX idx_buyer_created_id ON transactions(buyer_id, created_at DESC, id DESC);
CREATE INDEX idx_seller_created_id ON transactions(seller_id, created_at DESC, id DESC);
CREATE INDEX idx_status_created ON transactions(status_code, created_at);

-- transaction_items 테이블
CREATE INDEX idx_transaction_id ON transaction_items(transaction_id);

-- chat_rooms 테이블
CREATE INDEX idx_transaction_id ON chat_rooms(transaction_id);
```

**2) 쿼리 분할 검토**
- 현재: 단일 쿼리로 모든 정보 조회
- 대안: 메인 거래 목록 + 상세 정보 N+1 (필요시)
- 트레이드오프 분석 필요

**3) 실행 계획 분석**
```sql
EXPLAIN SELECT ... -- 현재 쿼리
```

#### 4.3.2 캐싱 전략

**캐싱 대상**:
- 거래 상태 코드 마스터 데이터 (transaction_status)
- 사용자 프로필 정보 (nickname, profile_image_url)

**구현 방안**:
```csharp
// IMemoryCache 또는 Redis 사용
services.AddMemoryCache();

// TransactionService.cs
public class TransactionService
{
    private readonly IMemoryCache _cache;

    private async Task<Dictionary<string, string>> GetStatusNamesAsync()
    {
        return await _cache.GetOrCreateAsync("TransactionStatusNames", async entry =>
        {
            entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromHours(24);
            return await _statusRepository.GetAllAsync();
        });
    }
}
```

#### 4.3.3 응답 크기 최적화

**문제점**:
- 불필요한 필드 전송 가능성
- 이미지 URL 최적화 미적용

**개선 방안**:
- 썸네일 이미지 크기 최적화 (CDN 파라미터 활용)
- 선택적 필드 조회 옵션 (fields 파라미터)

### 4.4 테스트 코드 작성 (우선순위: 높음)

#### 4.4.1 단위 테스트

**파일 구조**:
```
TicketPlatFormServer.Tests/
  ├── Services/
  │   └── Transaction/
  │       ├── TransactionServiceTests.cs
  │       └── TransactionServiceParameterValidationTests.cs
  ├── Repositories/
  │   └── Transaction/
  │       └── TransactionHistoryRepositoryTests.cs
  └── Controllers/
      └── TransactionControllerTests.cs
```

**테스트 시나리오**:

**TransactionServiceTests.cs**:
```csharp
[Fact]
public async Task GetPurchaseHistory_ValidParameters_ReturnsSuccess()
{
    // Arrange
    var mockRepo = new Mock<ITransactionHistoryRepository>();
    var service = new TransactionService(mockRepo.Object);

    // Act
    var result = await service.GetPurchaseHistoryAsync(
        userId: 1,
        status: "paid,confirmed",
        period: "1m",
        sortBy: "latest",
        cursor: null,
        limit: 20
    );

    // Assert
    Assert.NotNull(result);
    Assert.True(result.Items.Count <= 20);
}

[Fact]
public async Task GetPurchaseHistory_InvalidStatus_ThrowsAppException()
{
    // Arrange
    var service = new TransactionService(Mock.Of<ITransactionHistoryRepository>());

    // Act & Assert
    await Assert.ThrowsAsync<AppException>(() =>
        service.GetPurchaseHistoryAsync(1, "invalid_status", null, null, null, null)
    );
}

[Fact]
public async Task GetPurchaseHistory_ExceedsMaxLimit_UsesMaxLimit()
{
    // 50개 초과 요청 시 50개로 제한되는지 검증
}

[Fact]
public async Task GetPurchaseHistory_WithCursor_ReturnsNextPage()
{
    // 커서 기반 페이지네이션 동작 검증
}
```

#### 4.4.2 통합 테스트

**파일**: `IntegrationTests/TransactionApiTests.cs`

```csharp
public class TransactionApiTests : IClassFixture<WebApplicationFactory<Program>>
{
    [Fact]
    public async Task GET_Purchases_ReturnsOkWithData()
    {
        // Arrange
        var client = _factory.CreateClient();
        client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", _validToken);

        // Act
        var response = await client.GetAsync("/api/transactions/purchases");

        // Assert
        response.EnsureSuccessStatusCode();
        var content = await response.Content.ReadAsStringAsync();
        var result = JsonSerializer.Deserialize<TransactionHistoryRespDto>(content);

        Assert.NotNull(result);
        Assert.NotNull(result.Items);
    }

    [Fact]
    public async Task GET_Purchases_Unauthorized_Returns401()
    {
        // 인증 없이 호출 시 401 반환 검증
    }

    [Fact]
    public async Task GET_Purchases_WithInvalidStatus_Returns400()
    {
        // 잘못된 status 파라미터로 400 반환 검증
    }
}
```

#### 4.4.3 성능 테스트

**도구**: BenchmarkDotNet 또는 JMeter

**시나리오**:
- 동시 사용자 100명, 각 10회 요청
- 응답 시간 목표: 95 percentile < 500ms
- 처리량 목표: > 200 req/sec

---

## 5. 향후 개발 로드맵

### Phase 1: 안정화 (1-2주)
- [ ] 에러 처리 강화
  - [ ] 파라미터 유효성 검증 로직 추가
  - [ ] 예외 처리 추가 (Service, Repository)
  - [ ] 로깅 강화
- [ ] 단위 테스트 작성
  - [ ] TransactionService 테스트
  - [ ] Parameter Validation 테스트
- [ ] 통합 테스트 작성
  - [ ] API 엔드포인트 테스트

### Phase 2: 기능 확장 (2-3주)
- [ ] 추가 필드 지원
  - [ ] 환불 정보 (RefundedAt, RefundReason, RefundAmount)
  - [ ] 자동 확정 시각 (AutoConfirmAt)
  - [ ] 결제 방법 상세 (PaymentMethod, PaymentProvider)
- [ ] 필터링 옵션 추가
  - [ ] 티켓 카테고리별 필터
  - [ ] 가격 범위 필터
- [ ] API 명세 업데이트

### Phase 3: 성능 최적화 (2주)
- [ ] 쿼리 성능 개선
  - [ ] 실행 계획 분석
  - [ ] 인덱스 추가
  - [ ] 쿼리 최적화
- [ ] 캐싱 구현
  - [ ] 상태 코드 마스터 데이터 캐싱
  - [ ] 사용자 프로필 캐싱
- [ ] 성능 테스트 실시
  - [ ] 부하 테스트
  - [ ] 병목 지점 식별 및 개선

### Phase 4: 모니터링 및 운영 (1주)
- [ ] 로깅 및 모니터링
  - [ ] 구조화된 로그 추가 (Serilog)
  - [ ] 성능 메트릭 수집 (Application Insights)
  - [ ] 알림 설정 (에러율, 응답시간)
- [ ] 문서화
  - [ ] API 문서 업데이트 (Swagger)
  - [ ] 운영 가이드 작성

---

## 6. 참고 파일 목록

### 6.1 핵심 구현 파일
```
TicketPlatFormServer/
├── Controllers/
│   └── TransactionController.cs
├── Services/
│   └── Transaction/
│       ├── ITransactionService.cs
│       └── TransactionService.cs
├── Repository/
│   └── Transaction/
│       ├── ITransactionHistoryRepository.cs
│       └── TransactionHistoryRepository.cs
├── DTO/
│   └── Transaction/
│       └── TransactionHistoryRespDto.cs
└── DBModel/
    ├── Transaction.cs
    ├── TransactionItem.cs
    ├── TransactionHistory.cs
    └── TransactionStatus.cs
```

### 6.2 문서
```
TicketPlatFormServer/
└── api_spec/
    └── sales_purchases_api.md
```

### 6.3 설정 파일
```
TicketPlatFormServer/
├── Program.cs (DI 설정)
└── appsettings.json (데이터베이스 연결)
```

---

## 7. 관련 커밋 히스토리

- **0c3c9ac** (2026-02-05): `feat: 판매/구매 내역 조회 API 구현`
  - Controller, Service, Repository, DTO, DI 등록
  - 236줄의 복잡한 SQL 쿼리 구현
  - 커서 기반 페이지네이션 로직

- **244f7f7** (2026-02-05): `chore: 판매/구매 내역 API 명세 문서 추가`
  - API 요청/응답 예시
  - 파라미터 설명
  - Flutter UI 사용 시나리오

---

## 8. 결론

구매/판매 내역 조회 기능은 **핵심 기능이 모두 구현 완료**된 상태입니다. API 명세서에 정의된 요구사항을 충족하고 있으며, 커서 기반 페이지네이션과 다양한 필터링 옵션을 지원합니다.

다음 단계로는:
1. **에러 처리 강화** (파라미터 검증, 예외 처리)
2. **테스트 코드 작성** (단위 테스트, 통합 테스트)
3. **성능 최적화** (인덱싱, 캐싱)

위 항목들을 우선적으로 진행하여 안정성과 성능을 개선할 것을 권장합니다.

---

**문서 버전**: 1.0
**최종 수정일**: 2026-02-09
